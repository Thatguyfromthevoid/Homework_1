ip addr add 10.42.134.165/30 dev eth0   # R8-R9
ip addr add 10.42.64.1/19    dev eth1   # Calabria
ip addr add 10.42.134.169/30 dev eth2   # R8-R10

sysctl -w net.ipv4.ip_forward=1

# SUD antiorario: R8 → R10
ip route replace default via 10.42.134.170 dev eth2

cat /shared/etc_hosts >> /etc/hosts
